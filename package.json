{
  "name": "parcel-hyperapp-typescript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rd .cache/ ; ./node_modules/.bin/parcel ./src/index.html",
    "build": "rm -rd .cache/ ; ./node_modules/.bin/parcel build ./src/index.tsx --no-cache --no-minify -d ./build -o index.js",
    "electron": "./node_modules/.bin/electron main.js",
    "build-electron": "npm run build ; npm run electron",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.8.3",
    "parcel-bundler": "^1.6.2",
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "hyperapp": "^1.1.2"
  }
}
